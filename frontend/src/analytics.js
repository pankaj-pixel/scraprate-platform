const id = import.meta.env.VITE_GA_MEASUREMENT_ID;
export function initializeAnalytics(){ if(!id || typeof document==='undefined') return; try { const script=document.createElement('script'); script.async=true; script.src=`https://www.googletagmanager.com/gtag/js?id=${encodeURIComponent(id)}`; script.onerror=()=>{}; document.head.appendChild(script); window.dataLayer=window.dataLayer||[]; window.gtag=function(){window.dataLayer.push(arguments)}; window.gtag('js',new Date()); window.gtag('config',id,{anonymize_ip:true,send_page_view:true}); } catch { /* Analytics must never block rendering. */ } }
export function trackEvent(name, parameters={}){ try { if(id && typeof window.gtag==='function') window.gtag('event',name,parameters); } catch { /* Ignore analytics failures. */ } }
